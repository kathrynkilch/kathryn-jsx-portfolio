# Claude JSX Project

This small Vite + React scaffold is a place to paste a .jsx file exported from Claude.

How to use:

1. cd into the project folder
2. npm install
3. npm run dev

Replace `src/App.jsx` with your Claude .jsx file contents (or rename your file into `src/`).
